apiVersion: v1
kind: Service
metadata:
  name: message-broker-external
  namespace: discord-llm-agent
spec:
  type: NodePort
  selector:
    app: message-broker
  ports:
  - port: 5672
    targetPort: 5672
    nodePort: 30672
    name: broker-port
